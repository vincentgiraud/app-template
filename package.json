{
  "name": "app-template",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS web application template with AI agents, CI/CD, and compliance docs",
  "scripts": {
    "dev": "echo 'No app scaffolded yet. Run @Spec Planner to generate your project.'",
    "build": "echo 'No app scaffolded yet.'",
    "test": "echo 'No tests yet.' && exit 0",
    "lint": "echo 'No linter configured yet.' && exit 0"
  },
  "engines": {
    "node": ">=22"
  }
}
