name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - ".github/agents/**"
      - ".github/instructions/**"
      - ".github/prompts/**"
      - ".github/ISSUE_TEMPLATE/**"
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint-and-type-check:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        if: hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') != ''
        with:
          version: latest

      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: "22"

      - uses: astral-sh/setup-uv@v4
        if: hashFiles('uv.lock', 'pyproject.toml') != ''

      - uses: actions/setup-python@v5
        if: hashFiles('requirements.txt') != '' && hashFiles('uv.lock') == ''
        with:
          python-version: "3.12"

      - uses: actions/setup-dotnet@v4
        if: hashFiles('*.sln', '*.csproj') != ''
        with:
          dotnet-version: "9.0.x"

      - name: Install Node dependencies (pnpm — locked)
        if: hashFiles('pnpm-lock.yaml') != ''
        run: pnpm install --frozen-lockfile

      - name: Install Node dependencies (npm — locked)
        if: hashFiles('package-lock.json') != '' && hashFiles('pnpm-lock.yaml') == ''
        run: npm ci --ignore-scripts

      - name: Install Node dependencies (no lock file)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml') == '' && hashFiles('package-lock.json') == ''
        run: |
          if [ -f pnpm-workspace.yaml ]; then
            pnpm install
          else
            npm install
          fi

      - name: Install Python dependencies (uv)
        if: hashFiles('uv.lock') != ''
        run: uv sync --frozen

      - name: Install Python dependencies (pip)
        if: hashFiles('requirements.txt') != '' && hashFiles('uv.lock') == ''
        run: pip install -r requirements.txt
      - name: Install Python project (pip)
        if: hashFiles('pyproject.toml') != '' && hashFiles('requirements.txt') == '' && hashFiles('uv.lock') == ''
        run: pip install -e ".[dev]"

      - name: Restore .NET dependencies
        if: hashFiles('*.sln', '*.csproj') != ''
        run: dotnet restore

      - name: Lint (Node — pnpm)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') != ''
        run: pnpm run lint

      - name: Lint (Node — npm)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') == ''
        run: npm run lint

      - name: Lint (Python — uv)
        if: hashFiles('uv.lock') != ''
        run: uv run ruff check .

      - name: Lint (Python — pip)
        if: hashFiles('pyproject.toml', 'requirements.txt') != '' && hashFiles('uv.lock') == ''
        run: |
          pip install ruff 2>/dev/null || true
          if command -v ruff &>/dev/null; then
            ruff check .
          else
            echo "No Python linter configured — skipping"
          fi

      - name: Lint (.NET)
        if: hashFiles('*.sln', '*.csproj') != ''
        run: dotnet format --verify-no-changes || true

      - name: Type check (TypeScript)
        if: hashFiles('tsconfig.json') != ''
        run: npx tsc --noEmit

      - name: Type check (Python — uv)
        if: hashFiles('uv.lock') != ''
        run: uv run mypy . || echo "mypy not configured — skipping"

      - name: Type check (Python — pip)
        if: hashFiles('pyproject.toml', 'requirements.txt') != '' && hashFiles('uv.lock') == ''
        run: |
          if command -v mypy &>/dev/null; then
            mypy .
          else
            echo "mypy not installed — skipping Python type check"
          fi

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: lint-and-type-check
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        if: hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') != ''
        with:
          version: latest

      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: "22"

      - uses: astral-sh/setup-uv@v4
        if: hashFiles('uv.lock', 'pyproject.toml') != ''

      - uses: actions/setup-python@v5
        if: hashFiles('requirements.txt') != '' && hashFiles('uv.lock') == ''
        with:
          python-version: "3.12"

      - uses: actions/setup-dotnet@v4
        if: hashFiles('*.sln', '*.csproj') != ''
        with:
          dotnet-version: "9.0.x"

      - name: Install Node dependencies (pnpm — locked)
        if: hashFiles('pnpm-lock.yaml') != ''
        run: pnpm install --frozen-lockfile

      - name: Install Node dependencies (npm — locked)
        if: hashFiles('package-lock.json') != '' && hashFiles('pnpm-lock.yaml') == ''
        run: npm ci --ignore-scripts

      - name: Install Node dependencies (no lock file)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml') == '' && hashFiles('package-lock.json') == ''
        run: |
          if [ -f pnpm-workspace.yaml ]; then
            pnpm install
          else
            npm install
          fi

      - name: Install Python dependencies (uv)
        if: hashFiles('uv.lock') != ''
        run: uv sync --frozen

      - name: Install Python dependencies (pip)
        if: hashFiles('requirements.txt') != '' && hashFiles('uv.lock') == ''
        run: pip install -r requirements.txt
      - name: Install Python project (pip)
        if: hashFiles('pyproject.toml') != '' && hashFiles('requirements.txt') == '' && hashFiles('uv.lock') == ''
        run: pip install -e ".[dev]"

      - name: Restore .NET dependencies
        if: hashFiles('*.sln', '*.csproj') != ''
        run: dotnet restore

      - name: Test (Node — pnpm)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') != ''
        run: pnpm test

      - name: Test (Node — npm)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') == ''
        run: npm test

      - name: Test (Python — uv)
        if: hashFiles('uv.lock') != ''
        run: uv run pytest

      - name: Test (Python — pip)
        if: hashFiles('pyproject.toml', 'requirements.txt') != '' && hashFiles('uv.lock') == ''
        run: |
          if command -v pytest &>/dev/null; then
            pytest
          else
            echo "pytest not installed — skipping"
          fi

      - name: Test (.NET)
        if: hashFiles('*.sln', '*.csproj') != ''
        run: dotnet test --no-restore

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        if: hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') != ''
        with:
          version: latest

      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: "22"

      - uses: astral-sh/setup-uv@v4
        if: hashFiles('uv.lock', 'pyproject.toml') != ''

      - uses: actions/setup-python@v5
        if: hashFiles('requirements.txt') != '' && hashFiles('uv.lock') == ''
        with:
          python-version: "3.12"

      - uses: actions/setup-dotnet@v4
        if: hashFiles('*.sln', '*.csproj') != ''
        with:
          dotnet-version: "9.0.x"

      - name: Install Node dependencies (pnpm — locked)
        if: hashFiles('pnpm-lock.yaml') != ''
        run: pnpm install --frozen-lockfile

      - name: Install Node dependencies (npm — locked)
        if: hashFiles('package-lock.json') != '' && hashFiles('pnpm-lock.yaml') == ''
        run: npm ci --ignore-scripts

      - name: Install Node dependencies (no lock file)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml') == '' && hashFiles('package-lock.json') == ''
        run: |
          if [ -f pnpm-workspace.yaml ]; then
            pnpm install
          else
            npm install
          fi

      - name: Audit Node dependencies (pnpm)
        if: hashFiles('pnpm-lock.yaml') != ''
        run: pnpm audit --audit-level=high || true

      - name: Audit Node dependencies (npm)
        if: hashFiles('package-lock.json') != '' && hashFiles('pnpm-lock.yaml') == ''
        run: npm audit --audit-level=high || true

      - name: Audit Python dependencies (uv)
        if: hashFiles('uv.lock') != ''
        run: uv run pip-audit || true

      - name: Audit Python dependencies (pip)
        if: hashFiles('requirements.txt') != '' && hashFiles('uv.lock') == ''
        run: |
          pip install pip-audit 2>/dev/null || true
          if command -v pip-audit &>/dev/null; then
            pip-audit -r requirements.txt || true
          fi

      - name: Audit .NET dependencies
        if: hashFiles('*.sln', '*.csproj') != ''
        run: dotnet list package --vulnerable --include-transitive || true

      - name: Detect CodeQL languages
        id: codeql-langs
        run: |
          LANGS=""
          if find . -name '*.js' -o -name '*.ts' -o -name '*.tsx' -o -name '*.jsx' 2>/dev/null | grep -qv node_modules; then
            LANGS="javascript-typescript"
          fi
          if find . -name '*.py' 2>/dev/null | grep -q .; then
            [ -n "$LANGS" ] && LANGS="$LANGS,"
            LANGS="${LANGS}python"
          fi
          if find . -name '*.cs' 2>/dev/null | grep -q .; then
            [ -n "$LANGS" ] && LANGS="$LANGS,"
            LANGS="${LANGS}csharp"
          fi
          echo "languages=$LANGS" >> "$GITHUB_OUTPUT"
          echo "Detected CodeQL languages: ${LANGS:-none}"

      - name: Initialize CodeQL
        if: steps.codeql-langs.outputs.languages != ''
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ steps.codeql-langs.outputs.languages }}

      - name: Perform CodeQL Analysis
        if: steps.codeql-langs.outputs.languages != ''
        uses: github/codeql-action/analyze@v3

      - name: Semgrep SAST
        uses: returntocorp/semgrep-action@v1
        with:
          config: >-
            p/owasp-top-ten
            p/javascript
            p/typescript
            p/python
            p/csharp
            p/secrets
        env:
          SEMGREP_RULES: p/owasp-top-ten p/secrets
        continue-on-error: true

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint-and-type-check, test]
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        if: hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') != ''
        with:
          version: latest

      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: "22"

      - name: Install Node dependencies (pnpm — locked)
        if: hashFiles('pnpm-lock.yaml') != ''
        run: pnpm install --frozen-lockfile

      - name: Install Node dependencies (npm — locked)
        if: hashFiles('package-lock.json') != '' && hashFiles('pnpm-lock.yaml') == ''
        run: npm ci --ignore-scripts

      - name: Install Node dependencies (no lock file)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml') == '' && hashFiles('package-lock.json') == ''
        run: |
          if [ -f pnpm-workspace.yaml ]; then
            pnpm install
          else
            npm install
          fi

      - name: Build (pnpm)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') != ''
        run: pnpm run build

      - name: Build (npm)
        if: hashFiles('package.json') != '' && hashFiles('pnpm-lock.yaml', 'pnpm-workspace.yaml') == ''
        run: npm run build

      - name: Build (.NET)
        if: hashFiles('*.sln', '*.csproj') != ''
        run: dotnet publish --no-restore -c Release -o publish/

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        if: hashFiles('dist/**') != ''
        with:
          name: build-output
          path: dist/
          retention-days: 7

  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: [lint-and-type-check, test, security, build]
    if: always()
    steps:
      - name: Check job results
        run: |
          echo "## Quality Gate Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Lint & Type Check | ${{ needs.lint-and-type-check.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Tests | ${{ needs.test.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Security Scan | ${{ needs.security.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build | ${{ needs.build.result }} |" >> $GITHUB_STEP_SUMMARY

          if [[ "${{ needs.lint-and-type-check.result }}" == "failure" ]]; then
            echo "::error::Lint & Type Check failed"
            exit 1
          fi
          if [[ "${{ needs.test.result }}" == "failure" ]]; then
            echo "::error::Tests failed"
            exit 1
          fi
          if [[ "${{ needs.build.result }}" == "failure" ]]; then
            echo "::error::Build failed"
            exit 1
          fi
          echo "All required quality gates passed."
